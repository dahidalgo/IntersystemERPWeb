@model IEnumerable<Inspinia_MVC5.Models.FACTURA_DETALLE>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-sm-4">
        <h2>Index</h2>
        <ol class="breadcrumb">
            <li class="active">
                <strong>FACTURA_DETALLE</strong>
            </li>
        </ol>
    </div>
</div>


<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>List of FACTURA_DETALLE</h5>
					<div class="ibox-tools">
						@Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary btn-xs"})
					</div>
                </div>
                <div class="ibox-content">

<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CANTIDAD)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRECIO_FACTURA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ANULADA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FACTURA.CAUSA_ANULADA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRODUCTO.DESCRIPCION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CANTIDAD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRECIO_FACTURA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ANULADA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FACTURA.CAUSA_ANULADA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRODUCTO.DESCRIPCION)
        </td>
        <td>
			@Html.ActionLink("Details", "Details", new { id=item.DETALLE_ID }, new { @class = "btn btn-primary btn-sm"})
            @Html.ActionLink("Edit", "Edit", new { id=item.DETALLE_ID }, new { @class = "btn btn-white btn-sm"})
            @Html.ActionLink("Delete", "Delete", new { id=item.DETALLE_ID }, new { @class = "btn btn-white btn-sm"})
        </td>
    </tr>
}

</table>

                </div>
            </div>
        </div>
    </div>
 </div>



